module.exports = {
    name: " ==========> Interaction Commands <==========",
    aliases: ["==========> Interaction Commands <=========="],
    description:"",
    
    async execute (client, message, args, discord){
        try {

        } catch (error) {
            return console.log("Error : " + error);
        }
    }
};